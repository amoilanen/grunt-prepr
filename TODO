Implement the following:

- Processing files asynchronously, running task asynchronously (add a task option)
- Log detailed messages when required (add a task option)
- Support for other task runners such as Gulp
